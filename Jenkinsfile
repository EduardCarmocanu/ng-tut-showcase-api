pipeline {
    agent {
        docker {
            image 'node'
        }
    }
    stages {
        stage('deploy') {
            steps {
                sh 'echo Deployment is not ready yet, stay tuned'
            }
        }
    }
}